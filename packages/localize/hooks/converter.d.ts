export declare class Converter extends ConverterCommon {
  constructor(
    dataProvider: any,
    logger: any,
    platformData: any,
    projectData: any,
    androidResourcesMigrationService: any,
  ) {
  }

  run(): void;
}